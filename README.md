
  # Create multi-page product layout

  This is a code bundle for Create multi-page product layout. The original project is available at https://www.figma.com/design/xdAPlbnR83TLl4gIsTpg8L/Create-multi-page-product-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  

